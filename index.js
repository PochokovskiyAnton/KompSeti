class Chelik {
    constructor(name, email, ZIPcode) {
    this.id = lastName + (i++);
    this.email = email;
    this.ConEmail = Conemail;
    this.code = code;
    this.interface = `
    chelik ${this.counter}:
    Email is ${this.email},
    ConEmail is ${this.Conemail}
    Code is ${this.code}
    <p></p>
    `;
    }
  }